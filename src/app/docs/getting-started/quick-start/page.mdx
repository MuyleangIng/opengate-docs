import { Callout } from '@/components/docs/Callout';
import { CodeBlock } from '@/components/docs/CodeBlock';
import { Steps, Step } from '@/components/docs/Steps';

export const metadata = { title: 'Quick Start — OpenGate IAM' };

# Quick Start

Get OpenGate IAM running locally in under **5 minutes** using Docker Compose.

## On this page

- [Prerequisites](#prerequisites)
- [Start the Stack](#start-the-stack)
- [Verify Services](#verify-services)
- [First Login](#first-login)

---

## Prerequisites

| Requirement | Minimum Version |
|---|---|
| Docker | 24+ |
| Docker Compose | v2 |
| Java (local dev only) | 21 |
| Node.js (Admin Console only) | 20+ |

---

## Start the Stack

<Steps>
  <Step step={1} title="Clone the repository">
    <CodeBlock language="bash" code={`git clone https://github.com/MuyleangIng/opengate-iam.git
cd opengate-iam`} />
  </Step>

  <Step step={2} title="Start infrastructure (PostgreSQL · Redis · Kafka · Vault)">
    <CodeBlock language="bash" code={`docker compose -f docker/docker-compose.infra.yml up -d`} />

    Wait for PostgreSQL to be ready:

    <CodeBlock language="bash" code={`docker compose -f docker/docker-compose.infra.yml logs -f postgres`} />

    <Callout variant="info" title="Database initialisation">
      `init-databases.sh` automatically creates all required PostgreSQL databases on first startup.
    </Callout>
  </Step>

  <Step step={3} title="Start IAM microservices">
    <CodeBlock language="bash" code={`docker compose -f docker/docker-compose.services.yml up -d`} />

    Check all containers are healthy:

    <CodeBlock language="bash" code={`docker compose -f docker/docker-compose.services.yml ps`} />
  </Step>

  <Step step={4} title="Start the Admin Console (optional)">
    <CodeBlock language="bash" code={`cd opengate-console
npm install
npm run dev`} />

    Open [http://localhost:3000](http://localhost:3000) in your browser.
  </Step>
</Steps>

---

## Verify Services

Run a quick health check across all services:

<CodeBlock language="bash" code={`for port in 8080 8081 8082 8083 8084 8085 8086 8087 8088 8089; do
  status=$(curl -sf http://localhost:$port/actuator/health | python3 -c "import sys,json; print(json.load(sys.stdin)['status'])" 2>/dev/null || echo "DOWN")
  printf "Port %-5s → %s\n" $port "$status"
done`} />

Expected output when everything is healthy:

<CodeBlock language="text" code={`Port 8080  → UP
Port 8081  → UP
Port 8082  → UP
Port 8083  → UP
Port 8084  → UP
Port 8085  → UP
Port 8086  → UP
Port 8087  → UP
Port 8088  → UP
Port 8089  → UP`} />

---

## First Login

| Field | Value |
|---|---|
| Admin Console | `http://localhost:3000` |
| Username | `admin` |
| Password | `admin` |
| API Gateway | `http://localhost:8080` |
| Docs | `http://localhost:3001` |

<Callout variant="warning" title="Change the default admin password">
  The default `admin / admin` credentials are for local development only. Change them before exposing any endpoint to the internet.
</Callout>

### Get your first access token

<CodeBlock language="bash" code={`curl -s -X POST http://localhost:8080/realms/master/protocol/openid-connect/token \\
  -d "grant_type=client_credentials" \\
  -d "client_id=admin-cli" \\
  -d "client_secret=admin-secret" | jq .`} />

<Callout variant="success" title="You're up and running!">
  OpenGate IAM is live. Continue with [Installation](/docs/getting-started/installation) to learn about local development without Docker.
</Callout>
